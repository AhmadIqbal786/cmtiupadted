[build]
command = "echo 'No build needed for static site'"
publish = "."

[[redirects]]
from = "/*"
to = "/404.html"
status = 404

[dev]
publish = "."

[build.environment]
EXCLUDED_SOURCE_FILES = "node_modules, .env*, *.md"
